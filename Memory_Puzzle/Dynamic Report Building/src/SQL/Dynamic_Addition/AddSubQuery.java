package SQL.Dynamic_Addition;

/**
 * Created by Peter on 8/11/2016.
 */

// perhaps take everything so far, wrap it in " FROM () x"; only take complete statements (SELECT FROM required)

public class AddSubQuery implements Report {
    @Override
    public String addToSQL(String newCode) {
        return null;
    }
}
